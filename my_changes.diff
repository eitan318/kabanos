diff --git a/CMakeLists.txt b/CMakeLists.txt
index 1263321..4c1b87b 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -3,6 +3,7 @@ cmake_minimum_required(VERSION 3.16)
 project(myos C ASM_NASM)
 # Enable languages
 enable_language(C ASM_NASM)
+set(ARCH i686) 
 
 # Force 32-bit build
 set(CMAKE_C_FLAGS "-m32 -ffreestanding -fno-pic")
diff --git a/compile_commands.json b/compile_commands.json
new file mode 120000
index 0000000..9768b80
--- /dev/null
+++ b/compile_commands.json
@@ -0,0 +1 @@
+/home/magshimim/repos/1001_myos/build/compile_commands.json
\ No newline at end of file
diff --git a/docs/tips.md b/docs/tips.md
index d5d90da..3dfd449 100644
--- a/docs/tips.md
+++ b/docs/tips.md
@@ -1,100 +1,2 @@
-debugging: 
-disass page_table_destry
-
-
-max(CPL, RPL) ≤ DPL
-CPL - Current privlag level of CPU
-RPL - Requested Privlladge Level
-DPL - Descriptor Privlladge Level
-
-errcode selector - 0x18
-0000 0000 0001 1000
-
-GDTD = 0
-TI = GDT
-RPL = 0
-index = b11 = 3
-
-
-    0x0010791d
-    0x0000001b
-    0x00000202
-    0xbffff000
-    0x00000023
-
-
-taskB () at /home/magshimim/repos/1001_myos/src/kernel/process/schedualer.c:64
-(gdb) lay asm
-(gdb) x/16wx $esp
-0xc0001000:     Cannot access memory at address 0xc0001000
-(gdb)
-
-
-before esp change:
-
-(gdb) n
-(gdb) x/16wx $esp
-0x7a4c: 0x001083a4      0x00007b20      0x0007b000      0x00007a78
-0x7a5c: 0x00108373      0x0007b000      0x00114220      0x00000000
-0x7a6c: 0x00007b20      0x0007a003      0x00007aa4      0x00007ab8
-0x7a7c: 0x00102490      0x00007ac4      0x0007a000      0x00000003
-(gdb)
-
-after esp change to process kernel stack:
-
-(gdb) x/16wx $esp
-0xc0103fc0:     0x00000000      0x00000000      0x00000000      0x00000000
-0xc0103fd0:     0x00000000      0x00000000      0x00000000      0x00000000
-0xc0103fe0:     0x00000010      0x0000002d      0x00000000      0x001083aa
-0xc0103ff0:     0x0000001b      0x00000202      0xc0001000      0x00000023
-(gdb)
-
-
-after popa and pop ds and skip err and int:
-
-(gdb) x/16wx $esp
-0xc0103fec:     0x001083aa      0x0000001b      0x00000202      0xc0001000
-0xc0103ffc:     0x00000023      Cannot access memory at address 0xc0104000
-(gdb)
-
-PROB:
-  vmspace->pd = physical_access(pd_phys);
-  vmspace->pd_phys = pd_phys;
-  memset(vmspace->pd, 0, PAGE_SIZE);
-
-
-│    0xc0107d5a <kernel_vmspace_creat+30>    call   0xc010796f <physical_access>          │
-│    0xc0107d5f <kernel_vmspace_creat+35>    add    esp,0x10                              │
-│    0xc0107d62 <kernel_vmspace_creat+38>    mov    edx,DWORD PTR [ebp+0x8]               │
-│  > 0xc0107d65 <kernel_vmspace_creat+41>    mov    DWORD PTR [edx],eax                   │
-│    0xc0107d67 <kernel_vmspace_creat+43>    mov    eax,DWORD PTR [ebp+0x8]               │
-│    0xc0107d6a <kernel_vmspace_creat+46>    mov    edx,DWORD PTR [ebp-0xc]               │
-│    0xc0107d6d <kernel_vmspace_creat+49>    mov    DWORD PTR [eax+0x4],edx               │
-│    0xc0107d70 <kernel_vmspace_creat+52>    mov    eax,DWORD PTR [ebp+0x8]               │
-│    0xc0107d73 <kernel_vmspace_creat+55>    mov    eax,DWORD PTR [eax]                   │
-
-
-
-(gdb) p/x $edx
-$2 = 0xc0149000
-(gdb)
-
-TO THINK ABOUT:
-kmain(kernel_boot_info);     ■ Passing 'KernelBootInfo' to parameter of incompatible type 'void *'
-
-also, what is better? wraping in vspace struct? or allocating new page dir for vmspace? also how to alloc vmspace itself?
-
-also, should I copy the kernel params and free early pmm? 
-
-
-TODO:
-Check if stack created below kernel in entry is not overriden by pmm alocations
-
-
-b vmspace.c:53
-
-B+> 0xc0101000 <e9_putc>    add    eax,DWORD PTR [eax]       │
-│    0xc0101002 <e9_putc+2>  and    BYTE PTR [eax],al         │
-│    0xc0101004 <e9_putc+4>  add    edx,DWORD PTR [eax]       │
-│    0xc0101006 <e9_putc+6>  and    BYTE PTR [eax],al         │
-│ 
+process_create
+or vmspace create
diff --git a/ps b/ps
new file mode 100644
index 0000000..e69de29
diff --git a/scripts/kernel.gdb b/scripts/kernel.gdb
index eb02424..cddc9e0 100644
--- a/scripts/kernel.gdb
+++ b/scripts/kernel.gdb
@@ -3,8 +3,6 @@ target remote localhost:1234
 set architecture i386
 set disassembly-flavor intel
 b kmain 
-b preemptive_switch_isr_handler
-b switch_to
 
 c
 lay src
diff --git a/scripts/run.py b/scripts/run.py
index 1052aaa..f8f296d 100644
--- a/scripts/run.py
+++ b/scripts/run.py
@@ -17,20 +17,20 @@ is_debug = args.is_debug  # True if --is_debug was passed
 # QEMU command
 qemu_run_cmd = [
     "qemu-system-i386",
-    #    "-debugcon",
+    "-debugcon",
+    "stdio",  # This captures outb(0xe9, char)
     "-serial",
-    "stdio",
-    #    "-d",
-    #    "int",
+    "vc",  # Move serial to a virtual console to avoid stdio conflict
     "-drive",
     f"format=raw,file={OS_IMG}",
 ]
 
 qemu_debug_cmd = [
     "qemu-system-i386",
-    #    "-debugcon",
-    "-serial",
+    "-debugcon",
     "stdio",
+    "-serial",
+    "vc",
     "-drive",
     f"format=raw,file={OS_IMG}",
     "-s",
@@ -38,11 +38,10 @@ qemu_debug_cmd = [
 ]
 
 # Regex for the stack backtrace
-stack_regex = re.compile(r"STACK_OF_PANIC\[123\]:\s*((?:0x[0-9A-Fa-f]+\s*)+)")
+stack_regex = re.compile(r"STACK_OF_PANIC\[123\]: \s*((?:0x[0-9A-Fa-f]+\s*)+)")
 
 # Regex for the faulting instruction
-fault_regex = re.compile(r"FAULTING_INSTRUCTION_OF_PANIC\[123\]:\s*(0x[0-9A-Fa-f]+)")
-
+fault_regex = re.compile(r"FAULTING_INSTRUCTION_OF_PANIC\[123\]: \s*(0x[0-9A-Fa-f]+)")
 
 proc = subprocess.Popen(
     qemu_debug_cmd if is_debug else qemu_run_cmd,
@@ -61,7 +60,7 @@ stack_addrs = []
 for line in proc.stdout:
     print(line, end="")
 
-    detect_panic = False
+    detect_panic = True
     if not detect_panic:
         continue
 
diff --git a/src/kernel/CMakeLists.txt b/src/kernel/CMakeLists.txt
index 06d9316..2913e6c 100644
--- a/src/kernel/CMakeLists.txt
+++ b/src/kernel/CMakeLists.txt
@@ -1,26 +1,26 @@
 # Kernel CMakeLists.txt
 file(MAKE_DIRECTORY ${KERNEL_OUT_DIR})
 
-# Define include directories for kernel
-set(KERNEL_INCLUDE_DIRS
-    ${CMAKE_CURRENT_SOURCE_DIR}          # kernel/
-    ${SRC_DIR}/libs                       # src/libs/
-)
-
-# Glob kernel sources
-file(GLOB_RECURSE KERNEL_C_SOURCES 
-    ${CMAKE_CURRENT_SOURCE_DIR}/*.c 
-    ${SRC_DIR}/libs/*.c
-)
-
-# Remove kernel_symbols.c from the pre-link build
-list(FILTER KERNEL_C_SOURCES EXCLUDE REGEX "kernel_symbols\\.c$")
-file(GLOB_RECURSE KERNEL_ASM_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/*.asm)
-
-# Set NASM include directories
-set(NASM_INCLUDE_DIRS "-I${CMAKE_CURRENT_SOURCE_DIR} -I${SRC_DIR}/libs")
-
-# Process ASM sources
+# 1. Architecture Validation
+if(${ARCH} STREQUAL "i386" OR ${ARCH} STREQUAL "i686")
+    set(NASM_FORMAT elf32)
+    set(LINKER_TARGET elf_i386)
+else()
+    message(FATAL_ERROR "Architecture ${ARCH} not supported!")
+endif()
+
+# 2. Source Collection (Same as before)
+file(GLOB_RECURSE ALL_C_SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/*.c")
+file(GLOB_RECURSE LIB_SOURCES "${SRC_DIR}/libs/*.c")
+file(GLOB_RECURSE ARCH_SPECIFIC_SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/arch/${ARCH}/*.c")
+
+set(KERNEL_GENERIC_SOURCES ${ALL_C_SOURCES})
+list(FILTER KERNEL_GENERIC_SOURCES EXCLUDE REGEX "^${CMAKE_CURRENT_SOURCE_DIR}/arch/.*")
+
+set(KERNEL_C_SOURCES ${KERNEL_GENERIC_SOURCES} ${ARCH_SPECIFIC_SOURCES} ${LIB_SOURCES})
+
+# 3. ASM Processing (Same as before)
+file(GLOB_RECURSE KERNEL_ASM_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/arch/${ARCH}/*.asm)
 foreach(ASM_SOURCE ${KERNEL_ASM_SOURCES})
     get_filename_component(ASM_NAME ${ASM_SOURCE} NAME_WE)
     get_filename_component(ASM_DIR ${ASM_SOURCE} DIRECTORY)
@@ -30,124 +30,31 @@ foreach(ASM_SOURCE ${KERNEL_ASM_SOURCES})
     add_custom_command(
         OUTPUT ${ASM_OBJECT}
         COMMAND ${CMAKE_COMMAND} -E make_directory "${CMAKE_CURRENT_BINARY_DIR}/${ASM_REL_DIR}"
-        COMMAND nasm -f elf32 ${NASM_INCLUDE_DIRS} ${ASM_SOURCE} -o ${ASM_OBJECT} -g -F dwarf
+        COMMAND nasm -f ${NASM_FORMAT} ${ASM_SOURCE} -o ${ASM_OBJECT} -g -F dwarf
         DEPENDS ${ASM_SOURCE}
-        COMMENT "Compiling ASM: ${ASM_SOURCE}"
     )
     list(APPEND KERNEL_ASM_OBJECTS ${ASM_OBJECT})
 endforeach()
 
-# ============================================================================
-# STEP 1: Build kernel WITHOUT symbols (initial link)
-# ============================================================================
-# Create a stub symbols file for pre-link
-set(SYMBOLS_STUB ${CMAKE_CURRENT_BINARY_DIR}/symbols_stub.c)
-file(WRITE ${SYMBOLS_STUB} 
-"// Stub symbols for pre-link stage
-#include <stddef.h>
-
-typedef struct {
-    unsigned int addr;
-    char name[64];
-} symbol_t;
-
-const symbol_t symbols[] = {};
-const int symbol_count = 0;
-
-const char* lookup_symbol(unsigned int addr) {
-    return NULL;
-}
-")
-
-add_executable(kernel_pre.elf
-    ${KERNEL_C_SOURCES}
-    ${KERNEL_ASM_OBJECTS}
-    ${SYMBOLS_STUB}
-)
-
-set_target_properties(kernel_pre.elf PROPERTIES
-    LINKER_LANGUAGE C
-    RUNTIME_OUTPUT_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}
-)
-
-target_compile_options(kernel_pre.elf PRIVATE
-    -ffreestanding
-    -nostdlib
-    -g
-    -g3
-    -O0
-    -fno-omit-frame-pointer
-    -fno-inline
-)
-
-target_include_directories(kernel_pre.elf PRIVATE ${KERNEL_INCLUDE_DIRS})
-
-target_link_options(kernel_pre.elf PRIVATE
-    -nostdlib
-    LINKER:-m,elf_i386
-    LINKER:-T,${CMAKE_CURRENT_SOURCE_DIR}/linker.ld
-    LINKER:-Map=${CMAKE_CURRENT_BINARY_DIR}/kernel_pre.map
-)
-
-set_property(TARGET kernel_pre.elf APPEND PROPERTY LINK_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/linker.ld)
-
-# ============================================================================
-# STEP 2: Generate symbols.c from the pre-link map file
-# ============================================================================
-set(SYMBOLS_C ${CMAKE_CURRENT_BINARY_DIR}/symbols.c)
-
-add_custom_command(
-    OUTPUT ${SYMBOLS_C}
-    COMMAND ${Python_EXECUTABLE} ${CMAKE_SOURCE_DIR}/scripts/gen_symbols.py
-        ${CMAKE_CURRENT_BINARY_DIR}/kernel_pre.map
-        ${SYMBOLS_C}
-    DEPENDS kernel_pre.elf ${CMAKE_SOURCE_DIR}/scripts/gen_symbols.py
-    COMMENT "Generating symbols.c from kernel_pre.map"
-)
-
-
-# ============================================================================
-# STEP 3: Build final kernel WITH symbols
-# ============================================================================
-# For the final build, we need kernel_symbols.c - find it explicitly
-file(GLOB KERNEL_SYMBOLS_SOURCE ${CMAKE_CURRENT_SOURCE_DIR}/kernel_symbols.c)
-
-add_executable(kernel.elf
-    ${KERNEL_C_SOURCES}
-    ${KERNEL_ASM_OBJECTS}
-    ${SYMBOLS_C}  # Include the generated symbols
-    ${KERNEL_SYMBOLS_SOURCE}  # Now include kernel_symbols.c
-)
+# 4. Build Kernel
+add_executable(kernel.elf ${KERNEL_C_SOURCES} ${KERNEL_ASM_OBJECTS})
 
+# --- ADD THIS SECTION ---
 set_target_properties(kernel.elf PROPERTIES
-    LINKER_LANGUAGE C
-    RUNTIME_OUTPUT_DIRECTORY ${KERNEL_OUT_DIR}
-)
-
-target_compile_options(kernel.elf PRIVATE
-    -ffreestanding
-    -nostdlib
-    -g
+    RUNTIME_OUTPUT_DIRECTORY "${KERNEL_OUT_DIR}"
+    OUTPUT_NAME "kernel"
+    SUFFIX ".elf"
 )
 
-target_include_directories(kernel.elf PRIVATE ${KERNEL_INCLUDE_DIRS})
-
-target_link_options(kernel.elf PRIVATE
-    -nostdlib
-    LINKER:-m,elf_i386
-    LINKER:-T,${CMAKE_CURRENT_SOURCE_DIR}/linker.ld
-    LINKER:-Map=${KERNEL_OUT_DIR}/kernel.map
+# 5. The "Normal" Include Strategy
+# We point to the 'include' folder. 
+# Because that folder contains a subfolder 'arch', <arch/types.h> works!
+target_include_directories(kernel.elf PRIVATE 
+    ${CMAKE_CURRENT_SOURCE_DIR}                  # For "kernel/..."
+    ${CMAKE_CURRENT_SOURCE_DIR}/arch/${ARCH}/include # For "arch/..."
+    ${SRC_DIR}/libs                               # For libs
 )
 
-set_property(TARGET kernel.elf APPEND PROPERTY LINK_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/linker.ld)
-
-# ============================================================================
-# Export variables and create targets
-# ============================================================================
-set(KERNEL_ELF ${KERNEL_OUT_DIR}/kernel.elf)
-set(KERNEL_OUTPUT ${KERNEL_ELF} PARENT_SCOPE) 
-set(KERNEL_BIN_OUTPUT ${KERNEL_BIN} PARENT_SCOPE)
-
-add_custom_target(kernel ALL
-    DEPENDS ${KERNEL_ELF}
-)
+# 6. Options and Linker (Unchanged)
+target_compile_options(kernel.elf PRIVATE -ffreestanding -nostdlib -g -O0)
+target_link_options(kernel.elf PRIVATE -nostdlib LINKER:-m,${LINKER_TARGET} LINKER:-T,${CMAKE_CURRENT_SOURCE_DIR}/arch/${ARCH}/linker.ld)
diff --git a/src/kernel/arch/i686/hal.c b/src/kernel/arch/i686/hal.c
index 31ce72b..3115475 100644
--- a/src/kernel/arch/i686/hal.c
+++ b/src/kernel/arch/i686/hal.c
@@ -5,6 +5,7 @@
 #include "arch/i686/regs.h"
 #include "arch/i686/syscall.h"
 #include "arch/i686/timer.h"
+#include "arch/types.h"
 #include "assert.h"
 #include "panic.h"
 #include "string.h"
@@ -20,16 +21,36 @@ int hal_interrupts_state_get() {
   __asm__ volatile("pushf; pop %0" : "=r"(eflags));
   return eflags & 0x200;
 }
-int hal_regs_interrupt_number(struct regs *regs) {
+int hal_regs_interrupt_number(struct arch_regs *regs) {
   i686_regs_t *r = regs;
   return r->interrupt;
 }
-uintptr_t hal_regs_pc(struct regs *regs) {
+uintptr_t hal_regs_pc(struct arch_regs *regs) {
   i686_regs_t *r = regs;
   return r->eip;
 }
 
+uintptr_t hal_context_switch(vm_context_t *vm_context,
+                             thread_context_t *thread) {
+  uint32_t new_cr3 = vm_context->pd_phys;
+  void *new_esp = thread->esp;
+
+  __asm__ __volatile__("mov %0, %%cr3\n\t"
+                       "mov %1, %%esp\n\t"
+                       "pop %%ds\n\t"
+                       "popal\n\t"
+                       "add $8, %%esp\n\t"
+                       "iret\n\t"
+                       :
+                       : "r"(new_cr3), "r"(new_esp)
+                       : "memory");
+
+  // This part is never reached because iret changes execution flow
+  return 0;
+}
+
 void hal_serial_putc(const char c) { hal_out8(0x3f8, c); }
+void hal_debug_putc(const char c) { hal_out8(0xe9, c); }
 
 void hal_arch_init(void) {
   i686_gdt_init();
@@ -43,8 +64,8 @@ void hal_arch_init(void) {
 #define i686_MAX_REGS 16
 unsigned hal_regs_max_get() { return i686_MAX_REGS; }
 
-bool hal_regs_from_user(const struct regs *regs) {
-  i686_regs_t *r = regs;
+bool hal_regs_from_user(const struct arch_regs *regs) {
+  i686_regs_t *r = (i686_regs_t *)regs;
   return (r->cs & 0x3) != 0;
 }
 
@@ -82,7 +103,7 @@ int hal_describe_regs(i686_regs_t *regs, int max_regs, const char **names,
   return 16;
 }
 
-uintptr_t hal_backtrace(uintptr_t *data, struct regs *regs) {
+uintptr_t hal_backtrace(uintptr_t *data, struct arch_regs *regs) {
   i686_regs_t *arch_regs = regs;
   if (*data == 0) {
     if (regs)
diff --git a/src/kernel/arch/i686/hal_interrupts.c b/src/kernel/arch/i686/hal_interrupts.c
index b448d78..5d3bd59 100644
--- a/src/kernel/arch/i686/hal_interrupts.c
+++ b/src/kernel/arch/i686/hal_interrupts.c
@@ -125,5 +125,5 @@ void hal_irq_send_eoi(uint8_t irq) { i686_pic_send_eoi(irq); }
 //
 // ISR glue
 //
-extern void isr_dispatch(struct regs *);
-void i686_isr_handler(struct regs *regs) { isr_dispatch(regs); }
+extern void isr_dispatch(struct arch_regs *);
+void i686_isr_handler(struct arch_regs *regs) { isr_dispatch(regs); }
diff --git a/src/kernel/arch/i686/hal_vmm.c b/src/kernel/arch/i686/hal_vmm.c
index 90332fe..b3c4108 100644
--- a/src/kernel/arch/i686/hal_vmm.c
+++ b/src/kernel/arch/i686/hal_vmm.c
@@ -1,5 +1,7 @@
+#include "arch/types.h"
 #include "assert.h"
 #include "hal.h"
+#include "memory_management/kmalloc.h"
 #include "memory_management/memdefs.h"
 #include "memory_management/pmm.h"
 #include "stdio.h"
@@ -9,6 +11,7 @@
 #include <stddef.h>
 #include <stdint.h>
 
+typedef uint32_t page_dir_t;
 #define KERNEL_PD_START (KERNEL_BASE / (4 * 1024 * 1024))
 #define PD_PT_PRESENT PAGE_PRESENT
 #define PD_PT_READWRITE PAGE_READWRITE
@@ -25,6 +28,9 @@ static void tlb_flush_all(void) {
                    "mov %%eax, %%cr3\n" ::
                        : "eax", "memory");
 }
+static inline uint32_t *get_pd_ptr(vm_context_t *ctx) {
+  return (uint32_t *)ctx->pd;
+}
 
 static inline uint32_t get_pd_index(vaddr_t va) { return va >> 22; }
 
@@ -38,7 +44,8 @@ static inline uint32_t calc_remaining_in_pt(uint32_t pt_index) {
   return (PT_ENTRIES - pt_index) * PAGE_SIZE;
 }
 
-paddr_t hal_vm_virt_to_phys(page_dir_t *pd, vaddr_t va) {
+paddr_t hal_vm_virt_to_phys(vm_context_t *ctx, vaddr_t va) {
+  uint32_t *pd = get_pd_ptr(ctx);
   uint32_t pd_index = get_pd_index(va);
   uint32_t pt_index = get_pt_index(va);
 
@@ -184,8 +191,9 @@ static uint32_t calculate_map_size(uint32_t pt_index, vaddr_t va,
                                              : remaining_total;
 }
 
-bool hal_vm_map_range(page_dir_t *pd_virt, paddr_t pa_start, vaddr_t va_start,
+bool hal_vm_map_range(vm_context_t *ctx, paddr_t pa_start, vaddr_t va_start,
                       size_t size, uint32_t flags) {
+  uint32_t *pd_virt = get_pd_ptr(ctx);
   if (size == 0) {
     return true;
   }
@@ -225,7 +233,8 @@ bool hal_vm_map_range(page_dir_t *pd_virt, paddr_t pa_start, vaddr_t va_start,
   return true;
 }
 
-bool hal_vm_unmap_range(page_dir_t *pd_virt, vaddr_t va_start, size_t size) {
+bool hal_vm_unmap_range(vm_context_t *ctx, vaddr_t va_start, size_t size) {
+  uint32_t *pd_virt = get_pd_ptr(ctx);
   if (size == 0) {
     return true;
   }
@@ -268,33 +277,61 @@ bool hal_vm_unmap_range(page_dir_t *pd_virt, vaddr_t va_start, size_t size) {
 //
 // Single Page Operations
 //
-bool hal_vm_map(page_dir_t *pd_virt, vaddr_t va, paddr_t pa, uint32_t flags) {
-  bool res = hal_vm_map_range(pd_virt, pa, va, PAGE_SIZE, flags);
+bool hal_vm_map(vm_context_t *ctx, vaddr_t va, paddr_t pa, uint32_t flags) {
+  bool res = hal_vm_map_range(ctx, pa, va, PAGE_SIZE, flags);
   if (res) {
     tlb_flush(va);
   }
   return res;
 }
 
-bool hal_vm_unmap(page_dir_t *pd_virt, vaddr_t virt_addr) {
-  bool res = hal_vm_unmap_range(pd_virt, virt_addr, PAGE_SIZE);
+bool hal_vm_unmap(vm_context_t *ctx, vaddr_t virt_addr) {
+  bool res = hal_vm_unmap_range(ctx, virt_addr, PAGE_SIZE);
   if (res) {
     tlb_flush(virt_addr);
   }
   return res;
 }
 
-//
-// Page Directory Lifetime
-//
+void hal_vm_context_destroy(vm_context_t *ctx) {
+  if (!ctx)
+    return;
 
-void hal_vm_pd_destroy(page_dir_t *pd) {
+  // 1. Free all page tables in user space (0 to KERNEL_PD_START)
+  uint32_t *pd = get_pd_ptr(ctx);
   for (int i = 0; i < KERNEL_PD_START; i++) {
     if (pd[i] & PD_PT_PRESENT) {
-      paddr_t pt_phys = pd[i] & ~0xFFF;
-      pmm_frame_free(pt_phys);
+      pmm_frame_free(pd[i] & ~0xFFF);
     }
   }
+
+  // 2. Free the PD frame itself
+  pmm_frame_free(ctx->pd_phys);
+
+  // 3. Free the context struct
+  kfree(ctx);
 }
 
-paddr_t hal_vm_empty_pd_create() { return allocate_page_table(); }
+void hal_vm_context_create(vm_context_t *ctx) {
+  paddr_t pd_phys = pmm_frame_alloc();
+  if (!pd_phys)
+    return;
+  uint32_t *pd_virt = (uint32_t *)get_pt_virtual(pd_phys);
+
+  memset(pd_virt, 0, PAGE_SIZE);
+
+  ctx->pd = pd_virt;
+  ctx->pd_phys = pd_phys;
+}
+
+void hal_vm_context_clone(vm_context_t *src, vm_context_t *dst) {
+  int half_entries = PD_ENTRIES / 2;
+  memcpy(&dst->pd[half_entries], &src->pd[half_entries], (PAGE_SIZE / 2));
+}
+
+void hal_vm_context_switch(vm_context_t *ctx) {
+  if (!ctx)
+    return;
+  // Load the physical address of the page directory into CR3
+  __asm__ volatile("mov %0, %%cr3" ::"r"(ctx->pd_phys) : "memory");
+}
diff --git a/src/kernel/arch/i686/include/arch/types.h b/src/kernel/arch/i686/include/arch/types.h
new file mode 100644
index 0000000..8140c93
--- /dev/null
+++ b/src/kernel/arch/i686/include/arch/types.h
@@ -0,0 +1,15 @@
+#pragma once
+#include <stdint.h>
+
+// On i686, addresses are 32-bit
+typedef uint32_t arch_vaddr_t;
+typedef uint32_t arch_paddr_t;
+
+typedef struct thread_context_t {
+  void *esp; // On x86, we just need to save the stack pointer
+} thread_context_t;
+
+typedef struct vm_context_t {
+  uint32_t *pd;         // Pointer to Page Directory
+  arch_paddr_t pd_phys; // Physical address (to load into CR3)
+} vm_context_t;
diff --git a/src/kernel/linker.ld b/src/kernel/arch/i686/linker.ld
similarity index 100%
rename from src/kernel/linker.ld
rename to src/kernel/arch/i686/linker.ld
diff --git a/src/kernel/utils/math.asm b/src/kernel/arch/i686/math.asm
similarity index 100%
rename from src/kernel/utils/math.asm
rename to src/kernel/arch/i686/math.asm
diff --git a/src/kernel/arch/i686/regs.h b/src/kernel/arch/i686/regs.h
index f9d94d4..e31d01d 100644
--- a/src/kernel/arch/i686/regs.h
+++ b/src/kernel/arch/i686/regs.h
@@ -1,6 +1,7 @@
+#pragma once
 #include <stdint.h>
 
-typedef struct regs {
+typedef struct arch_regs {
   // in the reverse order they are pushed:
   uint32_t ds; // data segment pushed by us
   uint32_t edi, esi, ebp, esp_dummy, ebx, edx, ecx, eax; // pusha
diff --git a/src/kernel/arch/i686/syscall.c b/src/kernel/arch/i686/syscall.c
index 1e484ee..58afeec 100644
--- a/src/kernel/arch/i686/syscall.c
+++ b/src/kernel/arch/i686/syscall.c
@@ -1,11 +1,10 @@
 #include "syscall/syscall.h"
 #include "arch/i686/regs.h"
-#include "hal.h"
 #include "isr.h"
 
 #define SYSCALL_INTERRUPT 0x80
 
-static void syscall_isr_handler(struct regs *r) {
+static void syscall_isr_handler(i686_regs_t *r) {
   syscall_frame_t frame = {
       .num = r->eax, .args = {r->ebx, r->ecx, r->edx, r->esi, r->edi, r->ebp}};
 
diff --git a/src/kernel/arch/i686/timer.c b/src/kernel/arch/i686/timer.c
index a6a4ab5..d53d481 100644
--- a/src/kernel/arch/i686/timer.c
+++ b/src/kernel/arch/i686/timer.c
@@ -5,7 +5,7 @@
 #define TIMER_IRQ 0
 #define TIMER_INT 0x20
 
-static void timer_isr(struct regs *r) {
+static void timer_isr(struct arch_regs *r) {
   hal_irq_send_eoi(TIMER_IRQ); // Works only with eoi before
   sched_tick(r);
 }
diff --git a/src/kernel/drivers/keyboard.c b/src/kernel/drivers/keyboard.c
index 0ff690f..349bfe9 100644
--- a/src/kernel/drivers/keyboard.c
+++ b/src/kernel/drivers/keyboard.c
@@ -38,7 +38,7 @@ static char scancode_to_ascii_shift[128] = {
     'J', 'K', 'L',  ':',  '"',  '~', 0,   '|', 'Z', 'X', 'C', 'V',
     'B', 'N', 'M',  '<',  '>',  '?', 0,   '*', 0,   ' ', 0};
 
-static void keyboard_isr_handler(struct regs *regs) {
+static void keyboard_isr_handler(struct arch_regs *regs) {
   uint8_t scancode = hal_in8(KEYBOARD_PORT);
 
   // Handle key release
diff --git a/src/kernel/hal.h b/src/kernel/hal.h
index 76a9881..041fe24 100644
--- a/src/kernel/hal.h
+++ b/src/kernel/hal.h
@@ -1,25 +1,33 @@
 #pragma once
+#include <arch/types.h>
 #include <stdbool.h>
 #include <stddef.h>
 #include <stdint.h>
 
-struct regs;
-typedef void (*interrupt_handler_t)(struct regs *r);
+struct arch_regs;
+
+// In hal.h
+// #define HAL_VM_READ (1 << 0)
+// #define HAL_VM_WRITE (1 << 1)
+// #define HAL_VM_USER (1 << 2)
+// #define HAL_VM_NOCACHE (1 << 3)
+//
+typedef void (*interrupt_handler_t)(struct arch_regs *r);
 
 // Init
 void hal_arch_init(void);
 
 // Panic
-int hal_describe_regs(struct regs *regs, int max, const char **names,
+int hal_describe_regs(struct arch_regs *regs, int max, const char **names,
                       uintptr_t *values);
-uintptr_t hal_backtrace(uintptr_t *data, struct regs *regs);
+uintptr_t hal_backtrace(uintptr_t *data, struct arch_regs *regs);
 void hal_halt(void);
 void hal_trap();
 
 // Regs
-int hal_regs_interrupt_number(struct regs *regs);
-uintptr_t hal_regs_pc(struct regs *regs);
-bool hal_regs_from_user(const struct regs *regs);
+int hal_regs_interrupt_number(struct arch_regs *regs);
+uintptr_t hal_regs_pc(struct arch_regs *regs);
+bool hal_regs_from_user(const struct arch_regs *regs);
 unsigned hal_regs_max_get();
 
 // Interrupts
@@ -31,6 +39,7 @@ const char *hal_exception_name(int vector);
 
 // Debug
 void hal_serial_putc(const char c);
+void hal_debug_putc(const char c);
 
 // IO
 uint8_t hal_in8(uint16_t port);
@@ -43,9 +52,8 @@ void hal_irq_enable(int irq);
 void hal_irq_disable(int irq);
 void hal_irq_send_eoi(uint8_t irq);
 
-typedef uint32_t page_dir_t;
-typedef uint32_t vaddr_t;
-typedef uint32_t paddr_t;
+typedef arch_vaddr_t vaddr_t;
+typedef arch_paddr_t paddr_t;
 
 #define PAGE_PRESENT 0x1
 #define PAGE_READWRITE 0x2
@@ -56,14 +64,22 @@ typedef uint32_t paddr_t;
 #define PD_ENTRIES PAGE_SIZE / sizeof(uint32_t)
 
 // VMM
-bool hal_vm_map(page_dir_t *pd, vaddr_t va, paddr_t pa, uint32_t flags);
-bool hal_vm_unmap(page_dir_t *pd, vaddr_t va);
-paddr_t hal_vm_virt_to_phys(page_dir_t *pd, vaddr_t va);
-bool hal_vm_map_range(page_dir_t *pd_virt, paddr_t pa_start, vaddr_t va_start,
+// Context management
+void hal_vm_context_create(vm_context_t *ctx);
+void hal_vm_context_clone(vm_context_t *src, vm_context_t *dst);
+void hal_vm_context_destroy(vm_context_t *ctx);
+void hal_vm_context_switch(vm_context_t *ctx); // This loads CR3
+
+// Mapping logic
+bool hal_vm_map(vm_context_t *ctx, vaddr_t va, paddr_t pa, uint32_t flags);
+bool hal_vm_unmap(vm_context_t *ctx, vaddr_t va);
+bool hal_vm_map_range(vm_context_t *ctx, paddr_t pa_start, vaddr_t va_start,
                       size_t size, uint32_t flags);
-bool hal_vm_unmap_range(page_dir_t *pd_virt, vaddr_t va_start, size_t size);
-paddr_t hal_vm_empty_pd_create();
-void hal_vm_pd_destroy(page_dir_t *pd);
+
+bool hal_vm_unmap_range(vm_context_t *ctx, vaddr_t va_start, size_t size);
+
+// Info
+paddr_t hal_vm_virt_to_phys(vm_context_t *ctx, vaddr_t va);
 
 // Processes
 enum thread_mode { THREAD_MODE_KERNEL, THREAD_MODE_USER };
@@ -71,3 +87,6 @@ void hal_set_kernel_stack(int cpu_id, void *kstack_top);
 void *hal_build_initial_frame(void *kstack_top, uintptr_t entry,
                               uintptr_t user_stack, enum thread_mode mode,
                               int interrupt_number);
+
+uintptr_t hal_context_switch(vm_context_t *vm_context,
+                             thread_context_t *arch_thread);
diff --git a/src/kernel/hal_defaults.c b/src/kernel/hal_defaults.c
deleted file mode 100644
index 2aaada8..0000000
--- a/src/kernel/hal_defaults.c
+++ /dev/null
@@ -1,73 +0,0 @@
-#include "hal.h"
-#include <stddef.h>
-#include <stdint.h>
-
-#define weak __attribute__((__weak__))
-
-// Init
-void weak hal_arch_init(void) {}
-
-// Panic
-int weak hal_describe_regs(struct regs *regs, int max, const char **names,
-                           uintptr_t *values) {
-  return -1;
-}
-uintptr_t weak hal_backtrace(uintptr_t *data, struct regs *regs) {
-  return NULL;
-}
-
-void weak hal_halt(void) {}
-void weak hal_trap() {}
-
-// Regs
-int weak hal_regs_interrupt_number(struct regs *regs) { return -1; }
-uintptr_t weak hal_regs_pc(struct regs *regs) { return NULL; }
-bool weak hal_regs_from_user(const struct regs *regs) { return false; }
-unsigned weak hal_regs_max_get() { return -1; }
-
-// Interrupts
-void weak hal_interrupts_enable(void) {}
-void weak hal_interrupts_disable(void) {}
-int weak hal_interrupts_state_get() { return -1; }
-const char *weak hal_exception_name(int vector) {
-  return "hal_exception_name() not implemented";
-}
-
-// Debug
-void weak hal_serial_putc(const char c) {}
-
-// IO
-uint8_t weak hal_in8(uint16_t port) { return -1; }
-uint16_t weak hal_in16(uint16_t port) { return -1; }
-void weak hal_out8(uint16_t port, uint8_t value) {}
-void weak hal_out16(uint16_t port, uint16_t value) {}
-
-// IRQ
-void weak hal_irq_enable(int irq) {}
-void weak hal_irq_disable(int irq) {}
-void weak hal_irq_send_eoi(uint8_t irq) {}
-
-// VMM
-bool weak hal_vm_map(page_dir_t *pd, vaddr_t va, paddr_t pa, uint32_t flags) {
-  return false;
-}
-bool weak hal_vm_unmap(page_dir_t *pd, vaddr_t va) { return false; }
-paddr_t weak hal_vm_virt_to_phys(page_dir_t *pd, vaddr_t va) { return -1; }
-bool weak hal_vm_map_range(page_dir_t *pd_virt, paddr_t pa_start,
-                           vaddr_t va_start, size_t size, uint32_t flags) {
-  return false;
-}
-bool weak hal_vm_unmap_range(page_dir_t *pd_virt, vaddr_t va_start,
-                             size_t size) {
-  return false;
-}
-paddr_t weak hal_vm_empty_pd_create() { return -1; }
-void weak hal_vm_pd_destroy(page_dir_t *pd) {}
-
-// Processes
-void weak hal_set_kernel_stack(int cpu_id, void *kstack_top) {}
-void *weak hal_build_initial_frame(void *kstack_top, uintptr_t entry,
-                                   uintptr_t user_stack, enum thread_mode mode,
-                                   int interrupt_number) {
-  return NULL;
-}
diff --git a/src/kernel/isr.c b/src/kernel/isr.c
index cada3db..2a38b1e 100644
--- a/src/kernel/isr.c
+++ b/src/kernel/isr.c
@@ -11,9 +11,9 @@ void isr_handler_register(uint32_t interrupt_num, interrupt_handler_t handler) {
   g_isr_handlers[interrupt_num] = handler;
 }
 
-void isr_dispatch(struct regs *regs) {
-  int hal_regs_interrupt_number(struct regs * regs);
-  uintptr_t hal_regs_pc(struct regs * regs);
+void isr_dispatch(struct arch_regs *regs) {
+  int hal_regs_interrupt_number(struct arch_regs * regs);
+  uintptr_t hal_regs_pc(struct arch_regs * regs);
   int interrupt_num = hal_regs_interrupt_number(regs);
   if (g_isr_handlers[interrupt_num]) {
     g_isr_handlers[interrupt_num](regs);
diff --git a/src/kernel/isr.h b/src/kernel/isr.h
index a85f34b..315d8ca 100644
--- a/src/kernel/isr.h
+++ b/src/kernel/isr.h
@@ -1,4 +1,4 @@
 #pragma once
 #include "hal.h"
 void isr_handler_register(uint32_t interrupt_num, interrupt_handler_t handler);
-void isr_dispatch(struct regs *regs);
+void isr_dispatch(struct arch_regs *regs);
diff --git a/src/kernel/kernel_boot_info.c b/src/kernel/kernel_boot_info.c
index ece1882..4e13636 100644
--- a/src/kernel/kernel_boot_info.c
+++ b/src/kernel/kernel_boot_info.c
@@ -12,9 +12,7 @@ static uintptr_t early_alloc(unsigned len) {
   static unsigned idx = 0;
 
   if (idx + len >= EARLYALLOC_SIZE)
-    /* Return NULL on failure. It's too early in the boot process to give out a
-       diagnostic.*/
-    return NULL;
+    return -1;
 
   uint8_t *ptr = &buf[idx];
   idx += len;
@@ -23,7 +21,6 @@ static uintptr_t early_alloc(unsigned len) {
 }
 
 KernelBootInfo *parse_multiboot2_early(mb2_info_t *mbi) {
-
   KernelBootInfo *kernel_boot_info =
       (KernelBootInfo *)early_alloc(sizeof(*kernel_boot_info));
   uint8_t *tag_ptr = mbi->tags;
diff --git a/src/kernel/kmain.c b/src/kernel/kmain.c
index fba5570..8358c71 100644
--- a/src/kernel/kmain.c
+++ b/src/kernel/kmain.c
@@ -15,6 +15,7 @@
 #include "ut/ata/ata_ut_main.h"
 #include "ut/frame_allocator/frame_allocator_ut_main.h"
 #include "ut/keyboard_driver.h"
+#include "ut/paging/paging_ut_main.h"
 #include "utils/range.h"
 #include <stdbool.h>
 #include <stddef.h>
@@ -54,17 +55,17 @@ void kmain(uint32_t mb2_ptr) {
     return;
   }
 
-  // From now on no lower half mapping
   vmspace_switch(g_kernel_vmspace);
+  kmalloc_init();
+  ut_paging_main();
 
-  // Init hardware
   hal_arch_init();
   vga_clrscr();
   vga_setcursor(0, 0);
+
   kbd_init();
 
   hal_interrupts_enable();
-  kmalloc_init();
 
   if (!fat_initialize(34)) {
     debugf("Failed to initialize FAT\n");
diff --git a/src/kernel/loader/elf.c b/src/kernel/loader/elf.c
index effea5a..69c0aa3 100644
--- a/src/kernel/loader/elf.c
+++ b/src/kernel/loader/elf.c
@@ -16,9 +16,9 @@
 // Load segment: allocate, zero, and copy from file data
 // currently menually touch frame alloc and page_map
 // In future will be with VMA of process
-static int load_segment(page_dir_t *pd, vaddr_t va_start, size_t mem_size,
+static int load_segment(vm_context_t *ctx, vaddr_t va_start, size_t mem_size,
                         void *file_data, size_t file_size, uint32_t flags) {
-  if (!va_alloc_region(pd, va_start, mem_size, flags)) {
+  if (!va_alloc_region(ctx, va_start, mem_size, flags)) {
     return -1;
   }
 
@@ -28,7 +28,7 @@ static int load_segment(page_dir_t *pd, vaddr_t va_start, size_t mem_size,
   size_t file_copied = 0;
   for (vaddr_t page_va = pages_start; page_va < pages_end;
        page_va += PAGE_SIZE) {
-    paddr_t phys = hal_vm_virt_to_phys(pd, page_va);
+    paddr_t phys = hal_vm_virt_to_phys(ctx, page_va);
     ASSERT(phys);
 
     void *kva = kmap(phys);
@@ -64,9 +64,9 @@ static int load_segment(page_dir_t *pd, vaddr_t va_start, size_t mem_size,
 }
 
 // Load ELF file
-int elf_load(page_dir_t *pd, void *elf_data, uint32_t elf_size,
+int elf_load(vm_context_t *ctx, void *elf_data, uint32_t elf_size,
              uintptr_t *entry) {
-  ASSERT(pd && elf_data && entry);
+  ASSERT(ctx && elf_data && entry);
 
   if (elf_size < sizeof(ELFHeader)) {
     debugf("ELF: File too small\n");
@@ -118,7 +118,7 @@ int elf_load(page_dir_t *pd, void *elf_data, uint32_t elf_size,
     void *file_data =
         (ph->FileSize > 0) ? ((uint8_t *)elf_data + ph->Offset) : NULL;
 
-    if (load_segment(pd, ph->VirtualAddress, ph->MemorySize, file_data,
+    if (load_segment(ctx, ph->VirtualAddress, ph->MemorySize, file_data,
                      ph->FileSize, flags) != 0) {
       debugf("ELF: Failed to load segment %u\n", i);
       return -1;
diff --git a/src/kernel/loader/elf.h b/src/kernel/loader/elf.h
index 8998569..d2faf4e 100644
--- a/src/kernel/loader/elf.h
+++ b/src/kernel/loader/elf.h
@@ -83,5 +83,5 @@ enum ELFProgramFlags {
   ELF_PROGRAM_FLAG_READABLE = 0x4,
 };
 
-int elf_load(page_dir_t *page_dir, void *elf_data, uint32_t size,
+int elf_load(vm_context_t *page_dir, void *elf_data, uint32_t size,
              uintptr_t *entry);
diff --git a/src/kernel/memory_management/kmalloc.c b/src/kernel/memory_management/kmalloc.c
index d438122..42c9dac 100644
--- a/src/kernel/memory_management/kmalloc.c
+++ b/src/kernel/memory_management/kmalloc.c
@@ -1,4 +1,5 @@
 #include "kmalloc.h"
+#include "hal.h"
 #include "memory_management/memdefs.h"
 #include "memory_management/pmm.h"
 #include "memory_management/vmspace.h"
@@ -35,7 +36,7 @@ static kmalloc_stats_t stats = {0};
 
 static uint32_t next_heap_addr = KERNEL_HEAP_START;
 
-page_dir_t *kernel_page_directory;
+vm_context_t *kernel_vm_ctx;
 
 /**
  * Allocate a page from the kernel heap
@@ -53,7 +54,7 @@ static void *heap_page_alloc(void) {
 
   // Map it to virtual address
   uint32_t virtual = next_heap_addr;
-  if (!hal_vm_map(kernel_page_directory, virtual, physical, PAGE_READWRITE)) {
+  if (!hal_vm_map(kernel_vm_ctx, virtual, physical, PAGE_READWRITE)) {
     pmm_frame_free(physical);
     return NULL;
   }
@@ -67,10 +68,10 @@ static void *heap_page_alloc(void) {
  */
 static void heap_page_free(void *ptr) {
   uint32_t virtual = (uint32_t)ptr;
-  uint32_t physical = hal_vm_virt_to_phys(kernel_page_directory, virtual);
+  uint32_t physical = hal_vm_virt_to_phys(kernel_vm_ctx, virtual);
 
   if (physical) {
-    hal_vm_unmap(kernel_page_directory, virtual);
+    hal_vm_unmap(kernel_vm_ctx, virtual);
     pmm_frame_free(physical);
   }
 }
@@ -180,7 +181,7 @@ void kmalloc_init() {
   }
 
   extern vmspace_t *g_kernel_vmspace;
-  kernel_page_directory = g_kernel_vmspace->pd;
+  kernel_vm_ctx = g_kernel_vmspace->arch_context;
 }
 
 /**
diff --git a/src/kernel/memory_management/kmap.c b/src/kernel/memory_management/kmap.c
index d098bbf..b283475 100644
--- a/src/kernel/memory_management/kmap.c
+++ b/src/kernel/memory_management/kmap.c
@@ -9,7 +9,8 @@ extern vmspace_t *g_kernel_vmspace;
 
 void *kmap(uint32_t phys_addr) {
   // Map physical page into kernel PD
-  if (!hal_vm_map(g_kernel_vmspace->pd, kmap_temp, phys_addr, PAGE_READWRITE)) {
+  if (!hal_vm_map(g_kernel_vmspace->arch_context, kmap_temp, phys_addr,
+                  PAGE_READWRITE)) {
     debugf("kmap: failed to map phys 0x%x\n", phys_addr);
     return NULL;
   }
@@ -19,5 +20,5 @@ void *kmap(uint32_t phys_addr) {
 
 void kunmap(void) {
   // Unmap the page
-  hal_vm_unmap(g_kernel_vmspace->pd, kmap_temp);
+  hal_vm_unmap(g_kernel_vmspace->arch_context, kmap_temp);
 }
diff --git a/src/kernel/memory_management/va_allocation.c b/src/kernel/memory_management/va_allocation.c
index db19a74..3cd5aa4 100644
--- a/src/kernel/memory_management/va_allocation.c
+++ b/src/kernel/memory_management/va_allocation.c
@@ -4,7 +4,7 @@
 #include "stdio.h"
 #include "utils/math.h"
 
-bool va_alloc_region(page_dir_t *pd, uint32_t virt_start, size_t size,
+bool va_alloc_region(vm_context_t *ctx, uint32_t virt_start, size_t size,
                      uint32_t flags) {
   uint32_t start = align_down(virt_start, PAGE_SIZE);
   uint32_t end = align_up(virt_start + size, PAGE_SIZE);
@@ -14,31 +14,31 @@ bool va_alloc_region(page_dir_t *pd, uint32_t virt_start, size_t size,
     uint32_t phys = pmm_frame_alloc();
     if (!phys) {
       for (uint32_t j = 0; j < i; j++)
-        hal_vm_unmap(pd, start + j * PAGE_SIZE);
+        hal_vm_unmap(ctx, start + j * PAGE_SIZE);
       return false;
     }
 
-    if (!hal_vm_map(pd, start + i * PAGE_SIZE, phys, flags)) {
+    if (!hal_vm_map(ctx, start + i * PAGE_SIZE, phys, flags)) {
       pmm_frame_free(phys);
       for (uint32_t j = 0; j < i; j++)
-        hal_vm_unmap(pd, start + j * PAGE_SIZE);
+        hal_vm_unmap(ctx, start + j * PAGE_SIZE);
       return false;
     }
   }
   return true;
 }
 
-void va_free_region(page_dir_t *pd, uint32_t virt_start, size_t size) {
+void va_free_region(vm_context_t *ctx, uint32_t virt_start, size_t size) {
   uint32_t pages = (size + PAGE_SIZE - 1) / PAGE_SIZE;
 
   // Free physical frames before unmapping
   for (uint32_t i = 0; i < pages; i++) {
     uint32_t va = virt_start + i * PAGE_SIZE;
-    uint32_t phys = hal_vm_virt_to_phys(pd, va);
+    uint32_t phys = hal_vm_virt_to_phys(ctx, va);
     if (phys) {
       pmm_frame_free(phys);
     }
   }
 
-  hal_vm_unmap_range(pd, virt_start, size);
+  hal_vm_unmap_range(ctx, virt_start, size);
 }
diff --git a/src/kernel/memory_management/va_allocation.h b/src/kernel/memory_management/va_allocation.h
index 6f48e81..cca8836 100644
--- a/src/kernel/memory_management/va_allocation.h
+++ b/src/kernel/memory_management/va_allocation.h
@@ -1,10 +1,9 @@
 #pragma once
+#include "arch/types.h"
 #include <stdbool.h>
 #include <stddef.h>
 #include <stdint.h>
 
-#include "hal.h"
-
-bool va_alloc_region(page_dir_t *pd, uint32_t virt_start, size_t size,
+bool va_alloc_region(vm_context_t *pd, uint32_t virt_start, size_t size,
                      uint32_t flags);
-void va_free_region(page_dir_t *pd, uint32_t virt_start, size_t size);
+void va_free_region(vm_context_t *pd, uint32_t virt_start, size_t size);
diff --git a/src/kernel/memory_management/vmspace.c b/src/kernel/memory_management/vmspace.c
index 65b2f0c..c6f52e9 100644
--- a/src/kernel/memory_management/vmspace.c
+++ b/src/kernel/memory_management/vmspace.c
@@ -7,47 +7,41 @@
 #include "utils/range.h"
 #include <stddef.h>
 
-void kernel_vmspace_create(vmspace_t *vmspace, Range total_memory_range) {
-  vmspace->pd_phys = hal_vm_empty_pd_create();
-  if (!vmspace->pd_phys)
-    return;
-  vmspace->pd = (uint32_t *)vmspace->pd_phys;
+static vm_context_t g_kernel_ctx;
 
-  // Map to HIGHER HALF ONLY
-  hal_vm_map_range(vmspace->pd, total_memory_range.start,
-                   total_memory_range.start + KERNEL_BASE,
-                   total_memory_range.end, PAGE_READWRITE);
-
-  // Map VGA buffer BEFORE switching
-  hal_vm_map(vmspace->pd, VGA_SCREEN_BUF, VGA_SCREEN_BUF_PHYS, PAGE_READWRITE);
-  vmspace->pd = (uint32_t *)(vmspace->pd_phys + KERNEL_BASE);
+void kernel_vmspace_create(vmspace_t *vmspace, Range total_memory_range) {
+  hal_vm_context_create(&g_kernel_ctx);
+  vmspace->arch_context = &g_kernel_ctx;
+
+  // 2. Map the Kernel into the HIGHER HALF
+  // Use the new hal_vm_map_range which handles the pd internal logic
+  hal_vm_map_range(vmspace->arch_context,
+                   total_memory_range.start,               // Physical Start
+                   total_memory_range.start + KERNEL_BASE, // Virtual Start
+                   total_memory_range.end,                 // Size
+                   PAGE_READWRITE);
+
+  // 3. Map VGA buffer
+  hal_vm_map(vmspace->arch_context, VGA_SCREEN_BUF, VGA_SCREEN_BUF_PHYS,
+             PAGE_READWRITE);
 }
 
-// Create virtual memory space for user processes
 vmspace_t *vmspace_create() {
-  vmspace_t *vmspace = kmalloc(sizeof(*vmspace));
+  // 1. Allocate the generic vmspace container
+  vmspace_t *vmspace = kmalloc(sizeof(vmspace_t));
   if (!vmspace)
     return NULL;
 
-  vmspace->pd_phys = hal_vm_empty_pd_create();
-  if (!vmspace->pd_phys) {
-    kfree(vmspace);
-    return NULL;
-  }
-
-  // Temporarily map the new PD in current address space to initialize it
-  uint32_t *temp_pd = (uint32_t *)(vmspace->pd_phys + KERNEL_BASE);
+  vmspace->arch_context = kmalloc(sizeof(*vmspace->arch_context));
+  hal_vm_context_create(vmspace->arch_context);
 
-  // Copy kernel mappings while we're still in kernel's CR3
-  extern vmspace_t *g_kernel_vmspace;
-  memcpy(temp_pd, g_kernel_vmspace->pd, PAGE_SIZE);
+  hal_vm_context_clone(&g_kernel_ctx, vmspace->arch_context);
 
-  vmspace->pd = temp_pd;
   return vmspace;
 }
 
 void vmspace_switch(vmspace_t *vmspace) {
-  asm volatile("mov %0, %%cr3" ::"r"(vmspace->pd_phys));
+  hal_vm_context_switch(vmspace->arch_context);
 }
 
 void vmspace_destroy(vmspace_t *vmspace) {
@@ -56,9 +50,9 @@ void vmspace_destroy(vmspace_t *vmspace) {
   extern vmspace_t *g_kernel_vmspace;
 
   // kernel vmspace shall not be freed because it is early-kernel-allocated
-  if (vmspace->pd == g_kernel_vmspace->pd)
+  if (vmspace == g_kernel_vmspace)
     return;
 
-  pmm_frame_free(vmspace->pd_phys);
+  hal_vm_context_destroy(vmspace->arch_context);
   kfree(vmspace);
 }
diff --git a/src/kernel/memory_management/vmspace.h b/src/kernel/memory_management/vmspace.h
index 5a98b0d..2eb4b72 100644
--- a/src/kernel/memory_management/vmspace.h
+++ b/src/kernel/memory_management/vmspace.h
@@ -4,8 +4,7 @@
 #include <stdint.h>
 
 typedef struct vmspace_t {
-  uint32_t *pd;
-  paddr_t pd_phys;
+  vm_context_t *arch_context;
 } vmspace_t;
 
 // Create initial virtual memory space for kernel
diff --git a/src/kernel/panic.c b/src/kernel/panic.c
index f860e46..80fa0ce 100644
--- a/src/kernel/panic.c
+++ b/src/kernel/panic.c
@@ -13,7 +13,7 @@ void __attribute__((noreturn)) panic_halt(const char *fmt, ...) {
     hal_halt();
 }
 
-void panic_from_regs(struct regs *regs) {
+void panic_from_regs(struct arch_regs *regs) {
   int max_regs = hal_regs_max_get();
   const char *names[max_regs];
   uintptr_t vals[max_regs];
@@ -32,8 +32,8 @@ void panic_from_regs(struct regs *regs) {
     return;
   }
 
-  debugf("FAULTING_INSTRUCTION_OF_PANIC: 0x%lx\n", pc);
-  debugf("STACK_OF_PANIC:");
+  debugf("FAULTING_INSTRUCTION_OF_PANIC[123]: 0x%lx\n", pc);
+  debugf("STACK_OF_PANIC[123]:");
 
   uintptr_t state = 0;
   uintptr_t backtrace_pc;
diff --git a/src/kernel/panic.h b/src/kernel/panic.h
index c752e95..b35d117 100644
--- a/src/kernel/panic.h
+++ b/src/kernel/panic.h
@@ -1,3 +1,3 @@
 #include "hal.h"
-void panic_from_regs(struct regs *regs);
+void panic_from_regs(struct arch_regs *regs);
 void __attribute__((noreturn)) panic_halt(const char *fmt, ...);
diff --git a/src/kernel/proc/exec.c b/src/kernel/proc/exec.c
index e8f89e3..5f9ee45 100644
--- a/src/kernel/proc/exec.c
+++ b/src/kernel/proc/exec.c
@@ -13,14 +13,14 @@ int exec_load_elf(vmspace_t *vm, const char *path, uintptr_t *entry) {
   size_t size;
 
   fat_read_file(path, &data, &size);
-  int r = elf_load(vm->pd, data, size, entry);
+  int r = elf_load(vm->arch_context, data, size, entry);
   kfree(data);
   return r;
 }
 
 // Return top of stack
 uintptr_t setup_user_stack(vmspace_t *vm) {
-  if (!va_alloc_region(vm->pd, USER_STACK_BOTTOM + 1, USER_STACK_SIZE,
+  if (!va_alloc_region(vm->arch_context, USER_STACK_BOTTOM + 1, USER_STACK_SIZE,
                        PAGE_USER | PAGE_READWRITE)) {
     return -1;
   }
diff --git a/src/kernel/proc/proc.c b/src/kernel/proc/proc.c
index cdf2d57..cf74d17 100644
--- a/src/kernel/proc/proc.c
+++ b/src/kernel/proc/proc.c
@@ -11,8 +11,6 @@ process_t *process_create(void) {
   memset(p, 0, sizeof(*p));
 
   p->pid = alloc_pid();
-  p->vmspace = vmspace_create();
-
   return p;
 }
 
diff --git a/src/kernel/sched/context_switch.asm b/src/kernel/sched/context_switch.asm
deleted file mode 100644
index 941f306..0000000
--- a/src/kernel/sched/context_switch.asm
+++ /dev/null
@@ -1,27 +0,0 @@
-bits 32
-
-;
-; This should match the isr.asm common_isr for preeamptive switching
-;
-global switch_to
-switch_to:
-    ; Input: [esp + 4] = thread_t *next
-    mov eax, [esp + 4]          ; EAX = next thread
-    
-    ; Get process from thread
-    mov ebx, [eax + 4]          ; EBX = next->process
-    
-    ; Load CR3 from process->page_dir
-    mov ecx, [ebx + 4]          ; ECX = process->(vmspace*)
-    mov ecx, [ecx + 4]          ; ECX = process->vmspace->pd_phys 
-    mov cr3, ecx                ; Switch address space
-    
-    ; Load kernel ESP (points to saved interrupt frame)
-    mov esp, [eax + 8]          ; ESP = next->kernel_esp
-    
-    ; Restore CPU state from interrupt frame
-    pop ds                      
-    popa                        
-    add esp, 8                  ; Skip error code + interrupt number
-    iret                        
-
diff --git a/src/kernel/sched/sched.c b/src/kernel/sched/sched.c
index 0cd66c1..d91aa57 100644
--- a/src/kernel/sched/sched.c
+++ b/src/kernel/sched/sched.c
@@ -19,9 +19,7 @@ static thread_t *sched_next(void) {
   return tasks[current_index];
 }
 
-extern void __attribute__((naked)) switch_to(thread_t *p);
-
-void sched_tick(struct regs *r) {
+void sched_tick(struct arch_regs *r) {
   if (current == NULL) {
     current = sched_next();
   } else {
@@ -37,7 +35,7 @@ void sched_tick(struct regs *r) {
   uint32_t cpu_id = 0;
   hal_set_kernel_stack(cpu_id, next->kstack_top);
 
-  switch_to(next);
+  hal_context_switch(next->process->vmspace->arch_context, next->arch);
 }
 
 void sched_init(void) { current = NULL; }
diff --git a/src/kernel/sched/sched.h b/src/kernel/sched/sched.h
index 39cf4a9..b8c0c26 100644
--- a/src/kernel/sched/sched.h
+++ b/src/kernel/sched/sched.h
@@ -2,5 +2,5 @@
 #include "sched/thread.h"
 
 void sched_add(thread_t *t);
-void sched_tick(struct regs *r);
+void sched_tick(struct arch_regs *r);
 void sched_init();
diff --git a/src/kernel/sched/thread.c b/src/kernel/sched/thread.c
index 74c800e..d39af96 100644
--- a/src/kernel/sched/thread.c
+++ b/src/kernel/sched/thread.c
@@ -11,7 +11,7 @@ static uint32_t next_tid = 1;
 static uint32_t alloc_tid() { return next_tid++; }
 
 // Allocate and map kernel stack (in both kernel and user page directories)
-static void *alloc_kernel_stack(uint32_t tid, page_dir_t *user_pd) {
+static void *alloc_kernel_stack(uint32_t tid, vm_context_t *user_vm_ctx) {
   vaddr_t stack_bottom =
       PROCESS_KERNEL_STACKS_START + tid * PROCESS_KERNEL_STACK_SIZE;
   vaddr_t stack_top = stack_bottom + PROCESS_KERNEL_STACK_SIZE;
@@ -19,19 +19,19 @@ static void *alloc_kernel_stack(uint32_t tid, page_dir_t *user_pd) {
   extern vmspace_t *g_kernel_vmspace;
 
   /* Allocate in kernel page directory */
-  if (!va_alloc_region(g_kernel_vmspace->pd, stack_bottom,
+  if (!va_alloc_region(g_kernel_vmspace->arch_context, stack_bottom,
                        PROCESS_KERNEL_STACK_SIZE, PAGE_READWRITE)) {
     debugf("Failed to alloc kernel stack for tid %u\n", tid);
     return NULL;
   }
 
   /* Map same physical pages into user page directory if provided */
-  if (user_pd) {
+  if (user_vm_ctx) {
     for (vaddr_t va = stack_bottom; va < stack_top; va += PAGE_SIZE) {
-      paddr_t phys = hal_vm_virt_to_phys(g_kernel_vmspace->pd, va);
-      if (!phys || !hal_vm_map(user_pd, va, phys, PAGE_READWRITE)) {
+      paddr_t phys = hal_vm_virt_to_phys(g_kernel_vmspace->arch_context, va);
+      if (!phys || !hal_vm_map(user_vm_ctx, va, phys, PAGE_READWRITE)) {
         debugf("Failed to map kernel stack into user PD at 0x%x\n", va);
-        va_free_region(g_kernel_vmspace->pd, stack_bottom,
+        va_free_region(g_kernel_vmspace->arch_context, stack_bottom,
                        PROCESS_KERNEL_STACK_SIZE);
         return NULL;
       }
@@ -55,9 +55,9 @@ thread_t *thread_create(process_t *proc, uintptr_t entry, uintptr_t user_stack,
   t->mode = mode;
 
   /* Allocate kernel stack (mapped to user PD if user mode) */
-  page_dir_t *user_pd =
-      (mode == THREAD_MODE_USER && proc) ? proc->vmspace->pd : NULL;
-  void *kstack_top = alloc_kernel_stack(t->tid, user_pd);
+  vm_context_t *user_context =
+      (mode == THREAD_MODE_USER && proc) ? proc->vmspace->arch_context : NULL;
+  void *kstack_top = alloc_kernel_stack(t->tid, user_context);
   if (!kstack_top) {
     kfree(t);
     return NULL;
@@ -96,14 +96,15 @@ void thread_destroy(thread_t *t) {
   if (t->kstack_top) {
     vaddr_t stack_bottom = (vaddr_t)t->kstack_top - PROCESS_KERNEL_STACK_SIZE;
     extern vmspace_t *g_kernel_vmspace;
-    va_free_region(g_kernel_vmspace->pd, stack_bottom,
+    va_free_region(g_kernel_vmspace->arch_context, stack_bottom,
                    PROCESS_KERNEL_STACK_SIZE);
 
     /* Also unmap from user PD if it was a user thread */
-    if (t->mode == THREAD_MODE_USER && t->process && t->process->vmspace->pd) {
+    if (t->mode == THREAD_MODE_USER && t->process &&
+        t->process->vmspace->arch_context) {
       for (vaddr_t va = stack_bottom; va < (vaddr_t)t->kstack_top;
            va += PAGE_SIZE) {
-        hal_vm_unmap(t->process->vmspace->pd, va);
+        hal_vm_unmap(t->process->vmspace->arch_context, va);
       }
     }
   }
diff --git a/src/kernel/sched/thread.h b/src/kernel/sched/thread.h
index 02a882b..ad310fd 100644
--- a/src/kernel/sched/thread.h
+++ b/src/kernel/sched/thread.h
@@ -12,6 +12,8 @@ typedef struct thread {
   enum thread_state { THREAD_READY, THREAD_RUNNING } state;
   enum thread_mode mode;
 
+  struct thread_context_t *arch;
+
   /* Stack tracking (for cleanup only) */
   void *kstack_top; // Top of kernel stack (for deallocation)
 
diff --git a/src/kernel/ut/paging/paging_ut.c b/src/kernel/ut/paging/paging_ut.c
index c5135ed..64cb18a 100644
--- a/src/kernel/ut/paging/paging_ut.c
+++ b/src/kernel/ut/paging/paging_ut.c
@@ -4,13 +4,15 @@
 #include "memory_management/vmspace.h"
 #include "ut/ut_framework.h"
 
-static page_dir_t *g_pd;
+// Change: g_pd is now a context pointer, not a raw page directory array
+static vm_context_t *g_ctx;
 static vmspace_t *g_test_vmspace;
 
 int ut_simple_mapping(void) {
-  if (!hal_vm_map(g_pd, 0x400000, 0x200000, PAGE_READWRITE))
+  // Pass the context pointer instead of g_pd
+  if (!hal_vm_map(g_ctx, 0x400000, 0x200000, PAGE_READWRITE))
     return UT_FAIL;
-  return (hal_vm_virt_to_phys(g_pd, 0x400000) == 0x200000) ? UT_PASS : UT_FAIL;
+  return (hal_vm_virt_to_phys(g_ctx, 0x400000) == 0x200000) ? UT_PASS : UT_FAIL;
 }
 
 int ut_multiple_mappings(void) {
@@ -23,22 +25,22 @@ int ut_multiple_mappings(void) {
   int n = sizeof(map) / sizeof(map[0]);
 
   for (int i = 0; i < n; i++)
-    if (!hal_vm_map(g_pd, map[i][0], map[i][1], PAGE_READWRITE))
+    if (!hal_vm_map(g_ctx, map[i][0], map[i][1], PAGE_READWRITE))
       return UT_FAIL;
 
   for (int i = 0; i < n; i++)
-    if (hal_vm_virt_to_phys(g_pd, map[i][0]) != map[i][1])
+    if (hal_vm_virt_to_phys(g_ctx, map[i][0]) != map[i][1])
       return UT_FAIL;
 
   return UT_PASS;
 }
 
 int ut_unmapping(void) {
-  if (!hal_vm_map(g_pd, 0x400000, 0x200000, PAGE_READWRITE))
+  if (!hal_vm_map(g_ctx, 0x400000, 0x200000, PAGE_READWRITE))
     return UT_FAIL;
-  if (!hal_vm_unmap(g_pd, 0x400000))
+  if (!hal_vm_unmap(g_ctx, 0x400000))
     return UT_FAIL;
-  return (hal_vm_virt_to_phys(g_pd, 0x400000) == 0) ? UT_PASS : UT_FAIL;
+  return (hal_vm_virt_to_phys(g_ctx, 0x400000) == 0) ? UT_PASS : UT_FAIL;
 }
 
 int ut_identity_mapping(void) {
@@ -46,13 +48,13 @@ int ut_identity_mapping(void) {
 
   for (uint32_t i = 0; i < n; i++) {
     uint32_t addr = i * PAGE_SIZE;
-    if (!hal_vm_map(g_pd, addr, addr, PAGE_READWRITE))
+    if (!hal_vm_map(g_ctx, addr, addr, PAGE_READWRITE))
       return UT_FAIL;
   }
 
   for (uint32_t i = 0; i < n; i += 100) {
     uint32_t addr = i * PAGE_SIZE;
-    if (hal_vm_virt_to_phys(g_pd, addr) != addr)
+    if (hal_vm_virt_to_phys(g_ctx, addr) != addr)
       return UT_FAIL;
   }
 
@@ -60,45 +62,50 @@ int ut_identity_mapping(void) {
 }
 
 int ut_page_offset_preservation(void) {
-  if (!hal_vm_map(g_pd, 0x400000, 0x200000, PAGE_READWRITE))
+  if (!hal_vm_map(g_ctx, 0x400000, 0x200000, PAGE_READWRITE))
     return UT_FAIL;
 
   uint32_t off[] = {0, 1, 0x100, 0x500, 0xFFF};
   for (int i = 0; i < 5; i++)
-    if (hal_vm_virt_to_phys(g_pd, 0x400000 + off[i]) != 0x200000 + off[i])
+    if (hal_vm_virt_to_phys(g_ctx, 0x400000 + off[i]) != 0x200000 + off[i])
       return UT_FAIL;
 
   return UT_PASS;
 }
 
 int ut_unmap_nonexistent(void) {
-  hal_vm_unmap(g_pd, 0x400000);
-  return (hal_vm_virt_to_phys(g_pd, 0x400000) == 0) ? UT_PASS : UT_FAIL;
+  hal_vm_unmap(g_ctx, 0x400000);
+  return (hal_vm_virt_to_phys(g_ctx, 0x400000) == 0) ? UT_PASS : UT_FAIL;
 }
 
 int ut_remap_page(void) {
-  if (!hal_vm_map(g_pd, 0x400000, 0x200000, PAGE_READWRITE))
+  if (!hal_vm_map(g_ctx, 0x400000, 0x200000, PAGE_READWRITE))
     return UT_FAIL;
-  if (!hal_vm_map(g_pd, 0x400000, 0x300000, PAGE_READWRITE))
+  if (!hal_vm_map(g_ctx, 0x400000, 0x300000, PAGE_READWRITE))
     return UT_FAIL;
-  return (hal_vm_virt_to_phys(g_pd, 0x400000) == 0x300000) ? UT_PASS : UT_FAIL;
+  return (hal_vm_virt_to_phys(g_ctx, 0x400000) == 0x300000) ? UT_PASS : UT_FAIL;
 }
 
 int ut_different_page_tables(void) {
-  if (!hal_vm_map(g_pd, 0x400000, 0x100000, PAGE_READWRITE))
+  if (!hal_vm_map(g_ctx, 0x400000, 0x100000, PAGE_READWRITE))
     return UT_FAIL;
-  if (!hal_vm_map(g_pd, 0x800000, 0x200000, PAGE_READWRITE))
+  if (!hal_vm_map(g_ctx, 0x800000, 0x200000, PAGE_READWRITE))
     return UT_FAIL;
 
-  uint32_t r1 = hal_vm_virt_to_phys(g_pd, 0x400000);
-  uint32_t r2 = hal_vm_virt_to_phys(g_pd, 0x800000);
+  paddr_t r1 = hal_vm_virt_to_phys(g_ctx, 0x400000);
+  paddr_t r2 = hal_vm_virt_to_phys(g_ctx, 0x800000);
   return (r1 == 0x100000 && r2 == 0x200000) ? UT_PASS : UT_FAIL;
 }
 
 static int suite_setup() {
   g_test_vmspace = vmspace_create();
-  g_pd = g_test_vmspace->pd;
-  if (!g_pd)
+  if (!g_test_vmspace)
+    return UT_FAIL;
+
+  // Assign the HAL context to our global test pointer
+  g_ctx = g_test_vmspace->arch_context;
+
+  if (!g_ctx)
     return UT_FAIL;
 
   vmspace_switch(g_test_vmspace);
@@ -106,9 +113,10 @@ static int suite_setup() {
 }
 
 static int suite_teardown() {
-  vmspace_destroy(g_test_vmspace);
   extern vmspace_t *g_kernel_vmspace;
+  // Switch back to kernel space BEFORE destroying the test space
   vmspace_switch(g_kernel_vmspace);
+  vmspace_destroy(g_test_vmspace);
   return 0;
 }
 
diff --git a/src/kernel/vfs.c b/src/kernel/vfs.c
index 30d8852..13e7060 100644
--- a/src/kernel/vfs.c
+++ b/src/kernel/vfs.c
@@ -17,7 +17,7 @@ size_t pvfs_write(int fd, uint8_t *buf, size_t size) {
   case VFS_FD_DEBUG:
     for (size_t i = 0; i < size; i++) {
       char c = buf[i];
-      hal_serial_putc(c);
+      hal_debug_putc(c);
     }
     return size;
   default:
